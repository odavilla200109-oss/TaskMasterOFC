# ─────────────────────────────────────────────────────────
#  TaskMaster Backend v2 — Variáveis de Ambiente
#  Copie este arquivo para .env e preencha os valores
# ─────────────────────────────────────────────────────────

PORT=3001
NODE_ENV=development

# JWT — gere um segredo forte com:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=TROQUE_POR_SEGREDO_FORTE_DE_64_BYTES

# Expiração do token (ex: 7d, 24h, 30m)
JWT_EXPIRES_IN=7d

# Google OAuth — obtenha em console.cloud.google.com
GOOGLE_CLIENT_ID=SEU_CLIENT_ID.apps.googleusercontent.com

# CORS — URLs do frontend separadas por vírgula
# Em produção: coloque a URL real do Vercel
ALLOWED_ORIGINS=http://localhost:5173

# Banco SQLite — caminho do arquivo
# Em produção no Railway: /app/data/taskmaster.db (com volume configurado)
DB_PATH=./data/taskmaster.db
