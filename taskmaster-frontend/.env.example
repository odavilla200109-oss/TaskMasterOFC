# ─────────────────────────────────────────────────────────
#  TaskMaster Frontend v2 — Variáveis de Ambiente
#  Copie este arquivo para .env.local e preencha os valores
#
#  ⚠️  Variáveis com prefixo VITE_ são expostas no browser.
#      NUNCA coloque segredos aqui.
# ─────────────────────────────────────────────────────────

# URL do backend (Railway em produção, localhost em dev)
VITE_API_URL=http://localhost:3001

# Google OAuth Client ID — obtenha em console.cloud.google.com
# Deve ser o mesmo Client ID configurado no backend
VITE_GOOGLE_CLIENT_ID=SEU_CLIENT_ID.apps.googleusercontent.com
