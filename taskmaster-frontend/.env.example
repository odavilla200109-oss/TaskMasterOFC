# ─────────────────────────────────────────────────────────
#  TaskMaster Backend v3 — Variáveis de Ambiente
# ─────────────────────────────────────────────────────────

PORT=3001
NODE_ENV=development

# JWT — gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=TROQUE_POR_SEGREDO_FORTE_DE_64_BYTES
JWT_EXPIRES_IN=7d

# Google OAuth
467395601161-npobfj6chacf6umqm58cur786ej4vfbc.apps.googleusercontent.com

# CORS — URLs do frontend separadas por vírgula
ALLOWED_ORIGINS=http://localhost:5173,https://usetaskmaster.vercel.app

# Banco SQLite
DB_PATH=./data/taskmaster.db

# Admin — para acessar /api/reports/admin
# Escolha uma string secreta forte
ADMIN_SECRET=illuminati10
